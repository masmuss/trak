pre-commit:
  parallel: true
  commands:
    svelte-check:
      glob: '*.{svelte,ts,js}'
      run: pnpm --filter @trak/web run check
    prettier:
      glob: '*.{js,ts,svelte,json,css,md}'
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
